{
  "name": "longos-sync",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test Supabase/tests/*.test.mjs Tests/*.test.mjs",
    "check:edge-domain": "node --check Supabase/functions/health-ingest/domain.mjs",
    "check:swift-core": "swiftc -swift-version 6 -typecheck LongOSSyncCore/*.swift",
    "check:swift-parse": "swiftc -frontend -parse LongOSSyncCore/*.swift LongOSSyncCoreTests/*.swift LongOSSync/App/*.swift LongOSSync/Backend/*.swift LongOSSync/Features/*.swift LongOSSync/HealthKit/*.swift LongOSSync/Persistence/*.swift LongOSSync/Security/*.swift LongOSSync/Sync/*.swift",
    "check": "npm test && npm run check:edge-domain && npm run check:swift-core && npm run check:swift-parse"
  },
  "devDependencies": {
    "@electric-sql/pglite": "0.5.4"
  }
}
